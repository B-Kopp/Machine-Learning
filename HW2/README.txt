The code for this assignment is stored at: https://github.com/B-Kopp/CS-7641-HW2
There are 4 files, 1 for the neural network weight search named `CS 7641 HW2.ipynb`. 
Also 1 each for the 3 fitness functions, they are named in the format
`CS 7641 HW2 Part 2-<Fitness Function>.ipynb`

To run the code you need to install Anaconda Jupyter Notebook or parse out the python code into a script.
It is written in Python 3.6

You also need to install the following modules:
mlrose `pip install mlrose`
numpy `conda install -c anaconda numpy`
pandas `conda install -c anaconda pandas`
scikit-learn `conda install scikit-learn`
time `conda install -c conda-forge time`
matplotlib `conda install -c conda-forge matplotlib`